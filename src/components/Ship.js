export default {
        ShipsX: function (size, bridge, result, orient) {
                return {
                    bridge: bridge,
                    size: size,
                    positions: result,
                    damage: [],
                    loss: false,
                    orient: orient
                    }

                }

    }